### Objetivo: 
- fazer carregar o header do feed
- a partir do ultimo feed selecionado ele deixa como selecionado ele identifica qual feed esta sendo utilizado e carrega os dados do feed com os filtros do feed especifico
- e mostra tambem no filtro os filtros do feed utilizado
- ao mudar o feed ele muda o conteudo a partir disso com filtro e na parte de filtro ele carrega os filtros utilizados naquele feed

### questao:
- o que o carregar header do feed carrega?
- - Nomedo feed, id do feed e o ultimo feed utilizado

- o que o ocorre no filtro e feed?
- - ao verificar que um feed esta selecionado ele carrega os dados neles de forma ativa inicialmente e apos isso ele utilizaria um evento de click pra ver se o item clicado e um botao de feed diferente do anterior

- O que carregar nos feed e filtros?
- - feed so precisa carregar o feed com os filtros dele
- - no filtros carregar as opcoes de filtro do feed

### problemas
### resultado:
